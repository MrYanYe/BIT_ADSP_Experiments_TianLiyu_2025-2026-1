

这是实验要求、代码及运行结果。现在请描述实验结果并分析实验结果，不要分太多层，给我段落，语句连贯有逻辑，不要像AI说的。


我想让N从256以2倍增到512 1024……一直到65536，画出相位估计的标准差随N的变化的图（注意标题处标明 标题，\n是什么方法，谁在变化，周期，噪声方差），3种方法每个方法都画一个子图，子图横向排列，自行调整子图的高度和宽度合适。给我完整代码。create_list和list_find我已经有代码了。


把结果保存到当下路径一个子文件夹里的一个excel表格（自行命名，并加上精确到秒的当前时间）里，3种方法放到一个表格里，从上到下分3块存，每块标明标题，各个参数
第一行 N
第二行 估计相位的均值
第三行 估计相位的方差
其他地方不变，给我完整代码


现在这个代码是N在变化，现在改成N为固定值，T从1增加到2.1，间隔0.1，同样画出相位估计的标准差随T的变化的图(注意改“谁”在变化，还有子图标题里的固定参数)，还有表格（注意改变表格里有关“N”的表述）。格式和现在一样。
并且我想让代码的通用性高一些，就是系统自己判断哪个变量是多个值，如果是，就以它为自变量画图和生成表格。至于这个变量是不是多个值我不用自己在代码里说。比如N[128,512,1024];T=1 就代表N是多个值。如果检测到有不只一个变量是多个值，那就报错。
然后不光是N和T，以后我还可能把其他变量设为数组将其变为自变量。所以我的建议是在一个地方存 可能是多个值的变量，在这个范围里判断。
给我完整代码


我想给V4画的图改一下，就像V3的图那样，同样画出相位估计的标准差随T的变化的图，注意改“谁”在变化，还有子图标题里的固定参数（N,T,noisevar中的固定参数）。给我V4修改后的完整代码


现在这个代码的DFT是DFT+映射表的优化方法。我现在想要结构清晰一些，把优化放在一个单独的函数文件里，也就是在现有的主代码里，如果我想要这个映射表优化，就加上这个函数，DFT就是优化的；不想要优化，就注释掉，DFT就是不优化的，代码一样可以运行。另外图和表里的文字也要改一下，如果加上优化的话，图和excel里就按照现在的写DFT+映射表，如果没有优化就只写DFT（这里不要写一个判断加两种情况这种屎山代码，聪明一点）。
list_find和create_list代码我已经有了，直接调用即可。给我单独的函数文件和修改后的主代码的完整代码。


我想在主代码里实现 useDFTMappingOptim=1，整体的DFT就是优化的；useDFTMappingOptim=0，DFT就是不优化的，代码一样可以运行.但现在好像不管useDFTMappingOptim=多少，DFT都是优化的。我该怎么修改


现在这个代码在 useDFTMappingOptim = 0 的情况下，代码里调用了 create_list，但生成的结果没有被用到。所以我想把create_list放到dft_mapping_opt这个函数文件里，在主代码里去掉。list_find和create_list代码我已经有了，直接调用即可。给我修改后的函数文件和主代码里要修改的地方，保证代码整体逻辑不变无错误



我想把保存到excel这一部分分出来，变成一个单独的函数文件，然后再主代码里调用。给我函数文件的完整代码及主代码里要改动的地方。


我想加一个功能，把保存的数据输出到命令行。并且在开头设置一个参数（不是输入函数的变量），如果是true就输出到命令行，如果不是就不输出到命令行。给我完整代码


将其转化为一个单独的函数文件，输入为N,phi_true，T，A，Noise_Variance，useDFTMappingOptim。使其能够直接在其他代码里调用并执行


V8中利用useDFTMappingOptim可以直接调控是否使用DFT优化，如果使用的话就调用dft_mapping_opt函数文件。现在我想把V1_1也改成V8这样的格式，dft_mapping_opt函数文件时现有的可以直接调用，也在开头设一个useDFTMappingOptim参数，也在子图的命名里区分是否是优化。给我完整代码


把V8中子图标题中的英文对照V4中的中文的地方改为中文。给我完整代码



我想在这里
if isempty(multiIdx)
    error('No swept variable found: set exactly one field in paramGrid to be an array.');
改为不报错了。如果所以的检测变量都是单变量，那么直接执行phase_estimation_fixed_coefficients函数（此函数为现有，你可直接调用），然后跳过后面的所有程序。给我要修改的地方


给这个程序加一幅新的总图，放在现在现有的图的前面，即加噪声后的信号长什么样（代码里已经实现了，即Signal_with_Noise）  ，注意标题，坐标轴，单位等别忘写，横纵坐标是什么先搞清楚，代码里有些复杂。其他不变。给我完整代码





这是实验目的、代码及运行结果。现在请描述实验结果并分析实验结果，不要分太多层，给我段落，语句连贯有逻辑，不要像AI说的。

简要解释一下原因，可以辅以数学公式原理来解读。
三种方法之间的差异原因也说









你会注意到在V8的主代码的这里
        if useDFTOptim
            % 调用外部的优化函数（如果存在），只需把必需项传进去
            est_dft(m) = dft_mapping_opt(Signal_with_Noise, N, T, PhaseStepDeg);
如果用户启用DFT优化，就会调用一个新的函数来计算。
现在我想换一个优化的DFT函数（参数和现在的可以不一样）
阅读我给你的论文《基于DFT的正弦波初相估计算法及误差分析-北京航空航天大学学报》，这个论文里介绍了介绍一个他自己的DFT改进算法。
现在我想让你把这个改进算法写进一个新的函数文件里，函数的输入参数自己选，并在主函数中        if useDFTOptim
            % 调用外部的优化函数（如果存在），只需把必需项传进去
这个位置调用函数。
注意不要有语法错误，代码逻辑严谨，遵从论文中的改进算法。
给我单独函数文件的完整代码。以及在V8主代码中的调用位置。




写出这段代码的主要流程，依据我给你的论文来说，但不要直接用公式xx这种序号，可以用简称。不要分太多层，给我段落，不要有AI味。


